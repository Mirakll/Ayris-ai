# Задача 37 — Глобальные хоткеи

**Этап:** F — Хоткеи и интеграция ядра | **Зависит от:** 04, 30 | **Разделы ТЗ:** 8.1 (вкладка 5), 13

## Цель
Слой глобальных горячих клавиш: регистрация через WinAPI, системные хоткеи помощника и пользовательские на команды, детекция конфликтов, захват комбинации в UI, работа в играх.

## Что сделать
1. Модель комбинации `Hotkey` и её парсер уже готовы — `utils/hotkeys.py` из задачи 30. Здесь его не переписывать и второй парсер не заводить: если нотации не хватает, дополняется существующий. Всё, что нужно этой задаче, — брать из него канонический вид комбинации и русскую подпись для UI.
2. `utils/hotkey_manager.py` — регистрация через `RegisterHotKey` WinAPI в выделенном потоке с собственным окном-приёмником сообщений (`WM_HOTKEY`), снятие через `UnregisterHotKey`, безопасное завершение потока. Разбор кодов ошибок WinAPI (`ERROR_HOTKEY_ALREADY_REGISTERED`) в понятное сообщение с указанием, кто именно занят.
3. Системные хоткеи помощника: PTT, переключение режима Wake Word, показать/скрыть оверлей, Mute микрофона, отмена текущего действия. Значения по умолчанию и переопределение из конфига; каждый хоткей публикует типизированное событие в event bus, а не вызывает подсистемы напрямую.
4. Пользовательские хоткеи на команды: подписка на триггеры типа `hotkey` из БД, `WM_HOTKEY` → запуск команды через движок макросов. Отключённая команда свой хоткей не занимает.
5. Детекция конфликтов: между своими хоткеями (одна комбинация на две команды) — при сохранении в UI, с указанием конфликтующей команды; с системными и чужими — по отказу `RegisterHotKey`, с предложением выбрать другую комбинацию. Список занятых комбинаций доступен вкладке 55.
6. Захват комбинации в UI: API «нажмите сочетание» — временный низкоуровневый перехват (`SetWindowsHookEx WH_KEYBOARD_LL`) на время диалога, возврат нормализованной комбинации, отсечение одиночных модификаторов и запрещённых клавиш, отмена по Esc.
7. Поведение PTT: нажал-держи-говори, отпустил — обработка. Отслеживание удержания и отпускания (`RegisterHotKey` даёт только нажатие — нужен опрос состояния клавиши или низкоуровневый хук), минимальная длительность удержания против случайных касаний, публикация `PttPressed` / `PttReleased`.
8. Работа в играх: опциональный бэкенд на `interception` driver / Raw Input за общим интерфейсом `HotkeyBackend` — включается в настройках, при отсутствии драйвера graceful-фоллбек на WinAPI с сообщением в UI.
9. Перерегистрация без рестарта: смена комбинации в настройках или включение/выключение команды применяются на лету (снять — зарегистрировать заново), реакция на события изменения конфига и hot reload команд.
10. Тесты: детект конфликта своих хоткеев, обработка отказа регистрации (замоканный WinAPI), логика PTT по искусственным событиям нажатия/отпускания, перерегистрация при смене настроек. Парсинг и нормализация комбинаций покрыты тестами задачи 30 и здесь не дублируются.

## Файлы
`src/ayris/utils/hotkey_manager.py`, `src/ayris/utils/hotkey_backends/winapi.py`, `hotkey_backends/interception.py`, `tests/unit/test_hotkey_manager.py`

## Осторожно
- `RegisterHotKey` привязан к потоку: регистрировать и снимать нужно из того же потока, где крутится цикл сообщений, иначе снятие молча не сработает.
- Глобальный низкоуровневый хук держать только на время захвата комбинации в UI: постоянный хук — это тормоза системы и повод для антивируса.
- Хоткеи не работают поверх окон, запущенных с более высокими правами; сказать об этом пользователю честно, а не молчать.
- `interception` требует установки драйвера и прав администратора — это опция, а не обязательное условие запуска.
- Разметка тестов: конфликты, отказ регистрации и логика PTT проверяются на подменённой обёртке и идут везде; всё, что реально дёргает `RegisterHotKey` и цикл сообщений, закрывать `pytest.mark.skipif(sys.platform != "win32")`, чтобы прогон в песочнице оставался зелёным. Сценарии с живым сеансом — низкоуровневый хук, нажатие поверх чужого окна, удержание PTT — `@pytest.mark.hardware`: на раннере CI это выполнить нечем.
- Живого рабочего стола на раннере нет, поэтому вызовы `RegisterHotKey` / `UnregisterHotKey` и разбор кодов ошибок вынести за тонкую обёртку, а над ней оставить логику: какая комбинация регистрируется, что делать при отказе, что снимается при выключении команды. Тогда всё это проверяется подменой обёртки со сверкой переданных аргументов, а не руками. Отдельным тестом закрыть понятную ошибку при недоступном бэкенде — драйвер `interception` не установлен, окно-приёмник сообщений не создалось.
- `interception` — внешняя зависимость с драйвером, на раннер её колесо не встанет: в `requirements-ci.txt` не добавлять, импорт делать отложенным внутри фабрики бэкенда. Если версия попадёт в `pyproject.toml`, пинить её там и в `requirements-ci.txt` одинаково, иначе упадёт `tests/unit/test_packaging.py`, который следит за расхождением.

## Готово когда
- [ ] Конфликт двух своих команд на одной комбинации обнаруживается до сохранения.
- [ ] Системные хоткеи (PTT, Wake toggle, оверлей, Mute, отмена) регистрируются и публикуют события — **проверяет CI на windows-раннере**
- [ ] Хоткей команды запускает её; выключение команды освобождает комбинацию — **проверяет CI на windows-раннере**
- [ ] Занятая системой комбинация даёт понятное сообщение, а не тихий отказ — **проверяет CI на windows-раннере**
- [ ] Смена комбинации в настройках перерегистрирует хоткей без рестарта — **проверяет CI на windows-раннере**
- [ ] Захват «нажмите сочетание» возвращает нормализованную комбинацию и отменяется по Esc — **проверяешь ты руками**: открыть диалог захвата, нажать сочетание, потом отменить по Esc.
- [ ] PTT реально работает по удержанию и хоткей срабатывает поверх чужого приложения — **проверяешь ты руками**: подержать PTT, потом нажать хоткей в браузере и в полноэкранной игре.
- [ ] Тесты, ruff, black и mypy зелёные.

## Промпт для нового чата
> Прочитай `AYRIS_SPEC.md`, `AYRIS_CONTEXT.md` и код проекта, затем выполни Задачу 37 из `tasks/37_hotkeys.md`: реализуй регистрацию глобальных хоткеев через `RegisterHotKey` WinAPI в потоке с циклом сообщений, системные хоткеи помощника (PTT, Wake toggle, оверлей, Mute, отмена) с публикацией событий, пользовательские хоткеи на команды, детекцию конфликтов своих и системных комбинаций, API захвата сочетания для UI, логику PTT «нажал-держи-говори», опциональный бэкенд `interception` для игр с фоллбеком и перерегистрацию без рестарта при смене настроек. Модель и парсер комбинаций уже есть в `utils/hotkeys.py` (задача 30) — используй его, второй парсер не заводи. Напиши тесты с замоканным WinAPI. Прогони ruff, mypy, pytest.
