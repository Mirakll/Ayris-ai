"""Шаги мастера первого запуска.

Каждый шаг — самостоятельный виджет-наследник :class:`~ayris.onboarding.wizard.WizardStep`.
Импортируются лениво фабрикой ``build_default_steps`` (см. ``ayris.onboarding.factory``),
поэтому здесь ничего не реэкспортируется — так тесты подключают ровно тот шаг,
который проверяют, не таща за собой аудио- и сетевые зависимости остальных.
"""

from __future__ import annotations
