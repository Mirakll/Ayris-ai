# Движки речи для джоба `test-models`. Ставится ПОВЕРХ requirements-ci.txt и
# только в одном джобе.
#
# Зачем отдельный файл. Три обычных джоба гоняют `-m "not hardware and not
# models"`: им хватает заглушек, и ставить ради этого гигабайт CTranslate2 на
# каждый из четырёх прогонов — деньги и время на ровном месте. Но восемь тестов
# проверяют то, чего заглушка проверить не может: что Vosk действительно
# распознаёт фикстуру, что Piper действительно выдаёт звук нужной длины, что
# openWakeWord вообще загружается. Пока эти тесты не шли нигде, кроме машины
# разработчика, в путях к моделям спокойно жили пять багов — и все пять всплыли
# в тот день, когда веса наконец скачали.
#
# ПРАВИЛО: версии обязаны совпадать с pyproject.toml, как и в остальных двух
# файлах. За этим следит tests/unit/test_packaging.py.
#
# Ставить ОБЯЗАТЕЛЬНО с `-c requirements-ci.txt`. Без ограничений pip тянет
# свежие scipy и scikit-learn (их требует openwakeword, хотя импортирует только
# в коде обучения), а те требуют numpy>=2.3 — и pip молча поднимает numpy
# поверх пина 1.26.4. Джоб при этом зеленеет, но проверяет уже не то окружение,
# в котором работает приложение. С файлом ограничений pip обязан подобрать
# версии под наш numpy или упасть с внятным конфликтом.
#
# piper-tts здесь можно: раньше он тянул piper-phonemize, у которого не было
# колёс под windows-раннеры, и установка падала на сборке из исходников. С
# версии 1.7 зависимости — только onnxruntime и pathvalidate, оба с колёсами.
openwakeword==0.6.0
vosk==0.3.45
faster-whisper==1.2.1
piper-tts==1.7.0
